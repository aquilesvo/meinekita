<%= simple_form_for(@current_user) do |f| %>
  <!-- [...] -->
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit, value: 'Upload photo' %>
  <!-- [...] -->
<% end %>
